# タスクリスト

## 🚨 タスク完全完了の原則

**このファイルの全タスクが完了するまで作業を継続すること**

### 必須ルール
- **全てのタスクを`[x]`にすること**
- 「時間の都合により別タスクとして実施予定」は禁止
- 「実装が複雑すぎるため後回し」は禁止
- 未完了タスク（`[ ]`）を残したまま作業を終了しない

---

## フェーズ1: インライン名前編集

- [x] `editingNameId`, `editingNameValue` state を ProfilesScreen に追加
- [x] `handleSaveName(p)` 関数を実装（PUT API 呼び出し + バリデーション）
- [x] 名前セルのレンダリングを条件分岐に変更
  - [x] 通常時: 名前テキスト + 鉛筆アイコン（✎）
  - [x] 編集時: input 要素 + 確定（✓）・キャンセル（✗）ボタン
- [x] Escape キーでキャンセル処理を追加

## フェーズ2: プロファイルコピー

- [x] `handleCopy(p)` 関数を実装（POST API 呼び出し）
- [x] 操作列に「コピー」ボタンを追加
- [x] グリッドの操作列幅を調整（`1fr` → `1.6fr`）

## フェーズ3: 動作確認

- [x] 名前インライン編集が正常に動作することを確認
  - [x] 鉛筆アイコンクリックで入力欄に切り替わる
  - [x] Enter / ✓ で保存され一覧が更新される（トースト「名前を更新しました」表示）
  - [x] Escape / ✗ でキャンセルされ元の名前に戻る
  - [x] 空白での保存が弾かれる（バリデーション実装済み）
- [x] コピーが正常に動作することを確認
  - [x] 「コピー」クリックで「○○ のコピー」プロファイルが作成される（トースト表示）
  - [x] キャリブレーションなし・パラメータが元と同一であることを確認

---

## 実装後の振り返り

### 実装完了日
2026-05-24

### 計画と実績の差分

**計画と異なった点**:
- なし。計画通り実装できた。

**新たに必要になったタスク**:
- なし

### 学んだこと

**技術的な学び**:
- Row コンポーネントが cells 配列の第2要素から gridTemplateColumns を自動生成するため、ヘッダと Row の幅指定を一致させる必要がある
- blur イベントによる自動保存は採用せず、明示的な ✓/✗ ボタンと Enter/Escape キーで操作する設計が安全

### 次回への改善提案
- コピー後に作成されたプロファイルの行をハイライト表示するとわかりやすい
