# 20260910時点での問題点まとめ

## 学習運転(運転モデル学習・PID自動適合)ができていない
**product-requirements.md**に記載されている機能要件4. 学習運転(運転モデル学習・PID自動適合)が全くできていない。実装はされているが、KPIを満たせていない
### プライマリーKPI（Must）
- **車速追従精度（最大逸脱）**: 全走行区間において速度偏差の絶対値が1.0km/hを超えないこと（例外なし）**最重要**
- **車速追従精度（通常）**: 全走行モード実行中の速度偏差（実車速 − 基準車速）絶対値の95パーセンタイルが0.4km/h以内であること（2026-07-16 変更: 生 10Hz 車速の計測変動が p95≈0.4km/h あり 0.2 は計測床未満のため、実測に合わせて 0.2→0.4）
- **振動抑制**: 全走行区間（加減速含む）において速度偏差の符号反転が5秒に1回以下であること
  ここでの「符号反転」は**偏差が ±0.3km/h を両側で超えた**往復のみを数える（2026-09-09 変更:
  CAN 車速は生値で 10Hz 隣接差の std が 0.22-0.25km/h あり、旧しきい値 0.05km/h では測定
  ノイズのゼロクロスを数えていた。実機 3ca20d43 では最大反転が「最も追従が良い 120km/h 巡航」
  で発生しており、追従精度 p95≤0.4km/h を満たすほど反転が増える＝両立不能な定義だった

## 前提資料といままでに対応した内容
何度も試行錯誤を繰り返してきたが、解決できてない。いままでに対応した内容は以下の資料にまとめている\
| 資料 | 内容 |
|---|---|
| `docs/product-requirements.md` | プロダクト要求定義書 ここをベースに開発 |
| `docs/Problem/report20260909_2.md` | 第3ラウンドで入れた修正の根拠（根本原因分析・制御パラメータ全一覧・仕様書突き合わせ） |
| `docs/Problem/report20260909.md` | 第2ラウンド |
| `docs/Problem/report20260908.md` | 第1ラウンド |
| `docs/memo.md` | 議論の経緯（時系列） |
| `docs/hardware.md` | ハード構成＋アクチュエータ仕様（2026-09-09 確定） |
| `docs/Problem/制御フロー.md` | `src/` の実装だけを根拠に車速追従の全経路を MECE に分解したもの |
| `docs/Phase/RPD_Phase_20260910_Round4.md` | 第4ラウンドの実装計画書 |

### 今回実施する内容
本番環境で組み込まれている制御が多いため、まずは、`tests/`環境で1つ1つ順を追って実施していきKPIを満たすことを目指す\
まず、研究開発段階では極端にシンプルにする\
```
             目標車速
                │
                ▼
          ┌──────────┐
          │ Feedforward│
          └─────┬────┘
                │
                ▼
             ＋ ─────────┐
                ▲        │
                │        ▼
          ┌─────┴─────┐
          │    PID     │
          └───────────┘
                │
                ▼
             effort
                │
                ▼
        Pedal Arbiter
          │          │
          ▼          ▼
       Accel       Brake
          │          │
          └────┬─────┘
               ▼
             車両
               │
               ▼
             車速
               │
               └───────────Feedback
```
まずこれで、

1. FFだけ\
どの程度追従できるか\
2. FF + P\
どの程度改善するか\
3. F + PI\
定常偏差が消えるか\
4. FF + PID
振動が増えるか。
5. deadzone / rate limit\
実機制約を入れると何が悪化するか\

という順番で確認

その後に追加するもの\
順番としては、

第1段階

FF + PID

↓

第2段階

FF + PID
+ deadzone compensation
+ saturation
+ anti-windup

↓

第3段階

FF + PID
+ actuator delay compensation
+ gain scheduling

↓

第4段階

FF + PID
+ ILC

くらいが良いです。

## コードの作成と実行手順
すべて`tests/`に作成すること\
進捗はターミナルに都度printし、現在何をやっているかユーザーが分かるようにすること\
走行中はターミナル(基準車速、実車速、偏差、Kp,Ki,Kd,その他)とmatplotlibを用いてグラフ出力すること\
グラフのレイアウトは、本番環境の自動走行画面と同じレイアウトにすること\
1軸目が車速(基準と実車速)で2軸目はアクセルとブレーキの開度

0. main関数の作成
    今回用の実行関数、引数によってどこまで実行するかを定義
    テスト用車両プロファイル`config_testVehicle.yaml`を作成
    - 以降で必要となるパラメータを一括でこのファイルに保存する(ユーザーも編集できるようにすること)
    - 最低限以下は必要
        - 最高速    140km/h
        - 最大減速G 0.4G
        - FF用の運転モデル(2次多項式モデル)保存先パス
        - Kp
        - Ki
        - Kd
        - その他(必要があれば都度記載)
1. 初期化コードの作成
    本番環境から初期化(サーボ通信チェック、エラー消去、CAN通信チェック、UPS通信チェック、アクチュエータ初期位置)コードを流用
2. 閉ループパターン走行関数の作成
    1. 本番環境で採用している2次多項式モデル作成用の閉パターン走行
    2. 走行結果を基にモデル作成\
        パラメータを`config_testVehicle.yaml`に保存
    **実行手順**
        - 手順1 → 走行前チェック → 手順2
3. FFだけでモード走行
    走行モード管理に保存されているWLTP(Low,Mid,Hi,ExHi)1800sのモードを走行
    結果を`results/`に0.1s刻みのcsvとして保存。収集するデータは本番環境のログを流用
    - **reportYYYYMMDD_RunFF.md**として文、表、図を用いてわかりやすくレポートを作成すること\
    **実行手順**
        - 手順1 → 走行前チェック → 手順3

    **手順2,3の結果を受けてデバック**\
    共有資料
    | 資料 | 内容 |
    |---|---|
    | `tests/research/results/report20260911_RunFF.md` | 手順3. 実施時のレポート |
    | `docs/Problem/ProblemReport_20260912.md` | 上記レポートを受けての問題点と調査手順を示した資料 |
    | `tests/research/results/report20260912_explanationFF.md` | FFモデルの説明資料 |
    | `tests/research/results/report20260912_analysisFF_model.md` | FFモデルの解析結果 |
    | `tests/research/results/report20260912_KAIZEN_process2,3.md` | 手順 2（閉ループパターン走行とモデル作成）・手順 3（FF のみでモード走行）の改善案 |

    上記資料を確認し手順2,3のコードを修正する\
    修正後、手順2,3を再度実機で実行して確認する\
    **reportYYYYMMDD_debag_process2,3.md**としてレポートを作成すること\
    - どこをどのように変更したのか？
    - 実機確認結果


4. Kpのみの適合
    本番環境のPID適合用モードパターンを流用してKpのみ適合(10回まで走行可。その中で一番車速追従精度の良い値を採用する)\
    適合中は手順2までで保存したパラメータを使って良い。手順2までで保存したパラメータの設定変更は不可
    アクセル用のKpとブレーキ用のKpを設定すること
    結果を`results/`に保存
    パラメータを`config_testVehicle.yaml`に保存\
    **実行手順**
        - 手順1 → 走行前チェック → 手順4
5. FF + Kpでモード走行
    走行モード管理に保存されているWLTP(Low,Mid,Hi,ExHi)1800sのモードを走行
    結果を`results/`に0.1s刻みのcsvとして保存。収集するデータは本番環境のログを流用
    - **reportYYYYMMDD_RunFF&Kp.md**として文、表、図を用いてわかりやすくレポートを作成すること\
    **実行手順**
    - 手順1 → 走行前チェック → 手順5
6. Kp + Kiの適合
    本番環境のPID適合用モードパターンを流用してKp + Ki適合(10回まで走行可。その中で一番車速追従精度の良い値を採用する)\
    適合中は手順2までで保存したパラメータを使って良い。手順2までで保存したパラメータの設定変更は不可
    アクセル用のKp + Kiとブレーキ用のKp + Kiを設定すること
    結果を`results/`に保存
    パラメータを`config_testVehicle.yaml`に保存\
    **実行手順**
        - 手順1 → 走行前チェック → 手順6
7. FF + Kp + Kiでモード走行
    走行モード管理に保存されているWLTP(Low,Mid,Hi,ExHi)1800sのモードを走行
    結果を`results/`に0.1s刻みのcsvとして保存。収集するデータは本番環境のログを流用
    - **reportYYYYMMDD_RunFF&Kp&Ki.md**として文、表、図を用いてわかりやすくレポートを作成すること
    **実行手順**
            - 手順1 → 走行前チェック → 手順7
8. Kp + Ki + Kdの適合
    本番環境のPID適合用モードパターンを流用してKp + Ki + Kd適合(10回まで走行可。その中で一番車速追従精度の良い値を採用する)\
    適合中は手順2までで保存したパラメータを使って良い。手順2までで保存したパラメータの設定変更は不可
    アクセル用のKp + Ki + Kdとブレーキ用のKp + Ki + Kdを設定すること
    結果を`results/`に保存
    パラメータを`config_testVehicle.yaml`に保存
    **実行手順**
            - 手順1 → 走行前チェック → 手順8
9. FF + Kp + Ki + Kdでモード走行
    走行モード管理に保存されているWLTP(Low,Mid,Hi,ExHi)1800sのモードを走行
    結果を`results/`に0.1s刻みのcsvとして保存。収集するデータは本番環境のログを流用
    - **reportYYYYMMDD_RunFF&Kp&Ki&Kd.md**として文、表、図を用いてわかりやすくレポートを作成すること
    **実行手順**
            - 手順1 → 走行前チェック → 手順9
10. 制約の追加
    先読み、ILCを検討

## 遵守事項
- 本番環境から流用できるコードはしてよいが、なるべくシンプルなコードにすること
    - `/src`の本番環境のコードを実行しないこと。実行環境はすべて`tests/`に記載すること
- 実行コードを提示すること
    - `docs/memo_codeRun.md`に実行方法を記載すること
- 議論した内容は`docs/memo.md`に記載すること
- 1度にすべて実装しないこと
    上記手順を踏んで、0. ができたらユーザーに確認。OKなら1.へ
- **reportYYYYMMDD_XXXX.md**としてMECEに文、表、図を用いてわかりやすくレポートを作成すること

## 今回のゴール
`tests/`環境でプライマリーKPI（Must）を満たすこと