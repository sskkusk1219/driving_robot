# プロジェクトメモリ

## 技術スタック

- 開発環境: Raspberry Pi OS 64-bit
- 言語: Python 3.13, PostgreSQL 15
  - 仮想環境: `.venv` 

## スペック駆動開発の基本原則

### 基本フロー

1. **ドキュメント作成**: 永続ドキュメント(`docs/`)で「何を作るか」を定義
2. **作業計画**: Plan Modeで「今回何をするか」を計画し、計画を作成
3. **実装**: 計画に従って実装し、進捗を随時更新
4. **検証**: テストと動作確認\
            - テストと動作確認は、Claudeで動作を確認し、完了したらユーザーも実行して確認してもらうこと
5. **更新**: 必要に応じてドキュメント更新

### 重要なルール

#### 実装前の確認

新しい実装を始める前に、必ず以下を確認:

1. CLAUDE.mdを読む
2. 関連する永続ドキュメント(`docs/`)を読む
3. Grepで既存の類似実装を検索
4. 既存パターンを理解してから実装開始

## ディレクトリ構造
アプリケーション全体の「何を作るか」「どう作るか」を定義:

### 永続的ドキュメント(`docs/`)
- **product-requirements.md** - プロダクト要求定義書
  - このドキュメントに記載されている内容は、ユーザーが実現したいことを定義する
  - 基本設計を記述
  - 頻繁に更新されない
  - プロジェクト全体の「北極星」

  #### 参照ドキュメント
  開発する際に参照するドキュメント群。頻繁に更新される。\
  ユーザーが思っているが、まだドキュメント化されていない内容を補完する。
  - **memo.md** - CLAUDEと議論した内容のメモや分析の振り返り
  - **memo_codeRun.md** - 実行コードのメモ
  - **RPD_PhaseXXX.md** - (`docs/Phase`)各Phaseの実装計画書。Phaseごとに作成する
  - **ProblemReport_YYYYMMDD.md** - (`docs/Problem`)ユーザーが確認している本番環境の問題点をまとめたドキュメント。日付ごとに作成する
  - **hardware.md** - (`docs/`)ハードウェア構成のドキュメント

#### 下書き・アイデア（`docs/ideas/`）
- 壁打ち・ブレインストーミングの成果物
- 技術調査メモ
- 自由形式（構造化は最小限）
- `/setup-project`実行時に自動的に読み込まれる

### テクニカルリファレンス（`docs/manuals`）
ユーザーが実現したいことに対して仕様書を読み込んで実現可能か否か、必要に応じて参照すること
- **DS_USB_RS485_CABLES.pdf** - rasberry piとPCON-CBを接続するケーブルのマニュアル
- **MODBUS(MJ0162-12A).pdf** - MODBUSの仕様書
- **PCON-CB(MJ0342-4G).pdf** - アクチュエータコントローラの仕様書
- **RCP6-ROD(MJ3751-2Q).pdf** - アクチュエータの仕様書

### 本番コード(`src/`)
- 本番コードは`src/`に配置する

### テストコード(`tests/`)
- テストコードは`tests/`に配置する

## 遵守事項
- いきなりすべてを実装しないこと
  1つ1つ実装を行い、エージェントが実行してOKとなったらユーザーにも実行してもらって確認してもらうこと
- **議論したい**と言われたときには、いきなり実行計画(Plan)を作らず、内容を深堀して議論してからPlanを作ること
  - 議論の際は、ドキュメントやコードを参照すること
- 権限について
  - 議論や計画作成は**opus**以上。実装は**sonnet**で行う(トークン節約のため)
    - 実装する際に**opus**が選択されていれば、サブエージェントを起動して**sonnet**で実装すること
  - コードや変数の説明など読めばわかる内容は**sonnet**で解説すれば良い
- 解説する際は、分かりやすく説明すること
  - 変数名や略語は意味が分かるようにする
- 仮想環境は`.venv`で統一し、依存関係は`requirements.txt`に明記する
- 各Phaseの実装が完了したら、ユーザーも動作確認を実施する。実行コードを提示すること
- 必要に応じてskillsを使用すること
- **report.md**を作成する際はMECEに文、表、図を用いてわかりやすくレポートを作成すること