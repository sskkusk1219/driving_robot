# タスクリスト: 走行モードページ修正

## タスク

- [x] T1: ModesScreen (一覧) をテーブル形式 (ModesA) に書き換え
- [x] T2: ModeCreate を 2カラムレイアウト (ModeCreate 参照デザイン) に書き換え
- [x] T3: CsvSpeedGraph コンポーネントを実装 (CSVデータからグラフ描画)
- [x] T4: ブラウザで動作確認・微調整 (サーバー起動・API疎通確認・ファイル配信確認)
- [x] T5: 一覧プレビューを MiniSpeedGraph (実データ) に変更 — 詳細API を並行取得
- [x] T6: CSVヘッダー列名の柔軟化 (バックエンド + フロントエンド)
- [x] T7: 一覧ローディング状態の改善 (loading フラグ追加)
- [x] T8: 走行モード・車両プロファイル両ページへ検索・ソート機能追加、インポートボタン削除
- [x] T9: ModeCreate プレビュー Box のラベル隠れ修正 (paddingTop 調整、overflow 除去)
- [x] T10: CsvSpeedGraph 横軸ラベル重なり解消 + 縦軸最大値をキリの良い値に切り上げ

## 実装後の振り返り

- **実装完了日**: 2026-05-22
- **計画と実績の差分**:
  - 当初計画 (T1〜T4) に加え、動作確認を通じて多数の改善が追加された
  - 一覧プレビューは当初 Hatch プレースホルダーで済ませる予定だったが、詳細API並行取得による実データ表示に変更
  - CSV列名柔軟化はバックエンド・フロントエンド両方の対応が必要だった
  - ローディング状態管理、検索・ソート、グラフの視覚的改善など追加要件が順次発生
- **学んだこと**:
  - React 18 の自動バッチにより `setModes` と `setDetailMap` を連続呼び出しすれば中間レンダリングを抑制できる
  - Box コンポーネントの `overflow: auto` は `position: absolute` なラベルをクリップする
  - SVG の `padB` を増やして目盛りラベルとタイトルを分離するのが確実
  - CSVカラム名の正規化 (記号除去 + 小文字化) で幅広い命名規則に対応可能
- **次回への改善提案**:
  - 一覧API に `preview_points` (間引いた速度列) を追加すれば詳細API の N 回呼び出しを不要にできる
  - グラフの Y 軸目盛りをもう少し細かく (4分割) するとより読みやすくなる
